# 设备操作备忘（测试时用）

## 解锁屏幕（用户 2026-09-20 授权）
重启 SystemUI / 锁屏之后，设备可能停在锁屏，需要解锁才能继续测试：

1. 点亮屏幕（`adb shell input keyevent KEYCODE_WAKEUP`）——若已亮则跳过；
2. 上滑：`adb shell input swipe <x> <y2> <x> <y1> 300`（从下往上滑，打开数字密码盘）；
3. 数字密码盘出现后输入 **0 8 0 8 0 8**：逐个点击数字键（不要用 `input text`，锁屏密码盘通常不吃键盘事件）；
   - 先用 `screencap` 截图，按截图里 0~9 圆点的实际坐标点击更稳；
4. 解锁后按需再启动要测的应用。

**密码：080808**（用户明确授权 agent 自行解锁）。
